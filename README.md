# deep-image-restoration
Deep learning pipeline for removing compression artifacts from images degraded by aggressive Fourier/Wavelet compression. Encoder-decoder architecture trained on paired (compressed, original) image data, evaluated with PSNR and SSIM metrics.
